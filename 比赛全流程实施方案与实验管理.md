# 比赛全流程实施方案与实验管理

## 1. 文档目标

本文档用于指导团队从零开始完成比赛项目，覆盖从 GitHub 建仓、资料整理、数据管理、标注修正、基线训练、实验迭代、推理加速、Docker 封装、研究报告到最终提交和决赛准备的完整流程。

本文档不是单纯的技术方案，而是一个项目执行方案。它最重视三件事：

1. 每一步工作都有明确产物；
2. 每次实验都可追踪、可复现、可比较；
3. 最终提交材料能从日常记录自然沉淀出来。

## 2. 项目根判断

### 2.1 任务本质

本项目是光学遥感卫星图像目标检测任务。

实际训练和评估必须按 **25 个细分类**处理：

- `0-3`：舰船类，包括航母、两栖舰、驱护舰、民船；
- `4-23`：飞机类，共 20 个型号；
- `24`：发射车，对应评分中的车辆大类。

不能把项目简化成三类检测。官方答疑明确说明，只有子类正确分类才认定为正确。

### 2.2 标注形式

训练标注为 YOLO 格式水平框：

```text
class_id x_center y_center width height
```

坐标是归一化坐标。

评测使用水平框，不要求提交旋转框。若使用旋转框模型，最终也必须转换成水平框结果。

### 2.3 评分核心

初赛刚性门槛：

| 指标 | 要求 |
| --- | --- |
| 召回率 | `>= 85%` |
| 虚警率/误检率 | `<= 20%` |
| 单幅 `10000 x 10000` 图像推理时间 | `<= 20 秒` |

大类评分口径来自评分方案 V1.6 和答疑：

```text
先在子类正确分类的基础上计算细分类指标
再聚合为舰船、飞机、车辆三个大类
最后对三个大类指标求平均形成刚性指标判断
```

项目本地评估必须同时输出：

- 25 类细分类 Recall/FDR；
- 舰船、飞机、发射车大类 Recall/FDR；
- 三大类平均 Recall/FDR；
- 单张大图推理时间。

### 2.4 官方答疑带来的硬约束

后续方案必须吸收以下答疑结论：

- 测试图像不是全部 `10000 x 10000`，只是最大可能到该尺寸，且少量为大图；
- 图像未经过大气校正，像素值是 DN 值，不提供辐亮度转换；
- 训练集来自多源卫星，存在域差异；
- 舰船测试图为灰度或全色图，飞机和发射车为彩色图；
- 测试时只给图像，不告诉图像属于哪个场景；
- 测试集会多重核对，目视能分辨的目标应被标注；
- 训练集可能存在漏标、错标，允许自行修正；
- 允许使用外部数据，但不能使用测试集；
- 允许多模型、WBF、Soft-NMS、TTA、重叠切片、FP16、ONNX、TensorRT；
- 官方不会自动合并重复框；
- 置信度阈值由团队自行决定；
- 当前验证集为空，需要自行划分；
- 后续可能要求 Docker 封装和统一接口。

## 3. GitHub 仓库建设流程

### 3.1 建仓目标

GitHub 仓库不是只存代码，它应承担四类职责：

1. 固定比赛资料和规则理解；
2. 管理代码、配置和推理接口；
3. 管理实验记录和结果复盘；
4. 沉淀研究报告和提交材料。

### 3.2 推荐目录结构

建议逐步整理为：

```text
.
├── README.md
├── docs/
│   ├── official_notes/
│   ├── project_plan/
│   ├── experiment_management/
│   └── report_materials/
├── configs/
│   ├── dataset/
│   ├── model/
│   ├── train/
│   └── infer/
├── src/
│   ├── data/
│   ├── models/
│   ├── train/
│   ├── infer/
│   ├── eval/
│   └── utils/
├── scripts/
├── tools/
├── docker/
├── reports/
├── submissions/
├── experiments/
├── data/       # gitignore
├── runs/       # gitignore
├── weights/    # gitignore
└── outputs/    # gitignore
```

当前文档可以先放在仓库根目录。代码量变大后，再逐步移动到 `docs/` 目录。

### 3.3 不应提交到 GitHub 的内容

默认不提交：

- 官方训练集图像；
- 官方标注原始数据；
- 外部原始数据；
- 模型权重；
- 训练日志；
- TensorBoard、W&B、MLflow 原始输出；
- 大量预测可视化；
- 大图切片中间文件；
- 官方测试数据；
- 含个人手机号、邮箱、承诺书扫描件等敏感材料。

可提交：

- 类别映射；
- 数据检查脚本；
- 划分清单；
- 标注问题记录；
- 配置文件；
- 训练和推理代码；
- 评估代码；
- 实验摘要；
- 报告源文件；
- 提交材料清单。

### 3.4 分支管理

建议分支：

| 分支 | 用途 |
| --- | --- |
| `main` | 稳定版本，保证文档和代码可运行 |
| `dev` | 日常集成分支 |
| `feat/data-pipeline` | 数据处理相关 |
| `feat/eval-metrics` | 官方指标复现 |
| `feat/inference` | 大图推理和后处理 |
| `exp/E001-baseline` | 重要实验分支 |
| `submission/initial` | 初赛提交候选版本 |
| `submission/final` | 最终提交版本 |

规则：

- 不在 `main` 上直接做大改；
- 每次重要实验绑定一个 commit；
- 提交候选版本打 tag，例如 `submission-20260901-v1`；
- 代码和文档不一致时，以当前提交对应的实验记录为准。

## 4. 从零到完赛的阶段流程

## 阶段 P0：资料冻结与规则统一

### 目标

让团队所有人对任务、类别、评分、提交和答疑结论形成一致理解。

### 输入

- 官方赛题方案；
- 评分方案 V1.6；
- 数据集说明；
- 官方答疑；
- 当前仓库 Markdown 文档。

### 必做事项

1. 确认任务是 25 类水平框检测；
2. 确认发射车是车辆大类唯一目标；
3. 确认子类正确才算正确；
4. 确认训练集标注可修正；
5. 确认外部数据允许使用；
6. 确认测试只给图像，不给场景类型；
7. 确认重复框官方不会自动合并；
8. 确认本地验证集需要自行划分。

### 产物

- `README.md`；
- `比赛全流程实施方案与实验管理.md`；
- `比赛数据集说明文档.md`；
- 类别映射表；
- 官方规则摘要。

### 退出标准

团队内部不再争论“按 3 类还是 25 类训练”“能否用外部数据”“是否要检测边界目标”等基础问题。

## 阶段 P1：数据入库与安全管理

### 目标

安全、清晰地管理官方数据和外部数据，避免误删、误改、误传。

### 必做事项

1. 保存官方原始数据为只读版本；
2. 不直接修改原始标注；
3. 建立数据版本目录；
4. 生成文件清单和哈希摘要；
5. 建立图像与标注一一对应检查；
6. 将官方数据目录加入 `.gitignore`；
7. 外部数据单独建目录并记录来源。

### 推荐目录

```text
data/
├── official_raw/
├── official_fixed/
├── external_raw/
├── external_processed/
├── splits/
└── manifests/
```

### 产物

- `data/manifests/official_raw_manifest.csv`；
- `data/manifests/file_missing_report.csv`；
- `外部数据来源记录.md`；
- 数据备份说明。

### 退出标准

任意成员都能知道当前使用的是哪个数据版本，且不会把官方数据提交到 GitHub。

## 阶段 P2：数据检查、可视化与标注复核

### 目标

发现训练集漏标、错标、异常框、类别混淆和边界目标问题，为训练建立可靠标签。

### 检查项目

- 图像是否能读取；
- `.txt` 标注是否存在；
- 类别 ID 是否在 `0-24`；
- YOLO 坐标是否在 `0-1`；
- `width` 和 `height` 是否大于 0；
- 转像素框后是否越界；
- 是否存在空标注；
- 是否存在疑似漏标；
- 是否存在错标；
- 是否存在极小框、极大框、长宽比异常框；
- 舰船子类是否混淆；
- 发射车是否和普通车辆混淆。

### 标注复核重点

1. 舰船类：民船漏标、小船、边界船、QHS/LQS 混淆；
2. 飞机类：20 个型号样本不均衡、型号相似；
3. 发射车：普通货车和发射车混淆；
4. 边界目标：半出镜但可辨目标应保留；
5. 模糊目标：肉眼无法分辨的目标可标为忽略或不参与。

### 产物

- `标注问题记录表.md`；
- `labels_v0_raw`；
- `labels_v1_fixed`；
- 数据统计图；
- 类别分布表；
- 目标尺寸分布表；
- 典型问题截图。

### 退出标准

至少完成一版可训练的修正标签，并能说明相较原始标签改了什么、为什么改。

## 阶段 P3：本地验证集划分

### 目标

建立可信的本地验证集，避免官方反馈不足时无法定位问题。

### 原则

- 不按切片随机划分；
- 优先按原图或场景划分；
- 保证舰船、飞机、发射车都在验证集中；
- 保证少数类尽量覆盖；
- 保留大图压力测试集；
- 划分清单固定版本。

### 推荐划分

```text
train: 70%-80%
val: 10%-20%
local_test: 10%
speed_test: 手工构造或挑选大尺寸图像
```

如果数据特别少，可采用多折验证，但最终仍需要固定一版主验证集。

### 产物

- `splits_v1_train.txt`；
- `splits_v1_val.txt`；
- `splits_v1_local_test.txt`；
- `splits_v1_speed_test.txt`；
- 划分说明。

### 退出标准

后续任何实验都必须写明使用哪个 split 版本。

## 阶段 P4：官方指标本地复现

### 目标

建立接近官方 V1.6 口径的本地评估工具。

### 必须支持

- 子类正确才算 TP；
- 车辆/发射车 IoU 阈值 `0.35`；
- 其他目标 IoU 阈值 `0.50`；
- 按 score 从高到低匹配；
- 一个预测框最多匹配一个真实框；
- 一个真实框最多被一个预测框匹配；
- 重复框计为 FP；
- 输出 25 类指标；
- 输出三大类指标；
- 输出三大类平均指标；
- 输出速度指标。

### 指标输出

每次实验至少输出：

| 指标层级 | 内容 |
| --- | --- |
| 细分类 | 25 类 Recall、FDR、TP、FP、FN |
| 大类 | 舰船、飞机、发射车 Recall/FDR |
| 刚性门槛 | 三大类平均 Recall、三大类平均 FDR、推理时间 |
| 诊断 | 最差类别、主要 FP 来源、主要 FN 来源 |

### 产物

- 评估脚本；
- 指标说明；
- 测试样例；
- 一份人工校验过的小样例结果。

### 退出标准

能对任意预测结果稳定输出本地指标，并能解释 TP、FP、FN 来源。

## 阶段 P5：基线模型闭环

### 目标

跑通 25 类训练、推理、评估、可视化和提交格式转换，不追求一开始最优。

### 推荐基线

优先选择工程成熟、速度较快的检测框架，例如 YOLO 系列或 RT-DETR 类模型。

基线阶段不要同时加入过多复杂策略。

### 必做事项

1. 准备 `data.yaml` 或等价类别配置；
2. 训练 25 类模型；
3. 在本地验证集上推理；
4. 转换预测结果为统一格式；
5. 计算 Recall/FDR；
6. 可视化 TP/FP/FN；
7. 记录实验编号。

### 第一版实验建议

```text
E20260825-001-baseline
模型：中等规模 YOLO/RT-DETR
数据：official_raw 或 labels_v1_fixed
输入尺寸：先使用框架默认或 1024/1280
增强：默认增强
切片：先跑普通图，再跑大图切片
目标：验证完整闭环
```

### 产物

- 第一版模型权重；
- 第一份实验记录；
- 第一份验证指标；
- 第一批误检/漏检图；
- 基线问题清单。

### 退出标准

任意成员能复现基线结果。

## 阶段 P6：数据修正与小样本不均衡处理

### 目标

解决训练集标注噪声、少数类不足、类间混淆问题。

### 数据修正策略

- 原始标签只读；
- 修正标签独立版本化；
- 每个修正要能追踪原因；
- 大规模修正前先小范围验证；
- 不确定类别集中复核；
- 舰船类优先建立判别样例库。

### 不均衡处理策略

可逐步尝试：

- 类别均衡采样；
- 少数类过采样；
- Repeat Factor Sampling；
- Focal Loss；
- Class-Balanced Loss；
- 少数类 Copy-Paste；
- 外部数据预训练；
- 易混淆类别专项增强。

### 实验原则

每次只验证一个主要假设，例如：

```text
E002：只验证 labels_v1_fixed 是否优于 raw
E003：只验证少数类过采样
E004：只验证 Copy-Paste
E005：只验证外部飞机数据预训练
```

不要一次同时改变标签、模型、输入尺寸、增强和阈值。

### 产物

- 标签版本对比表；
- 不均衡类别统计；
- 数据增强策略记录；
- 少数类指标提升记录。

### 退出标准

能证明数据修正或不均衡策略对某些类别有明确收益，且没有严重拉低其他类别。

## 阶段 P7：大图切片推理与结果融合

### 目标

支持任意尺寸图像，尤其是最大 `10000 x 10000` 压力场景。

### 推理流程

```text
输入图像
  -> 判断尺寸和通道
  -> 大图重叠切片
  -> 批量推理
  -> 切片坐标还原到原图坐标
  -> 分类别 NMS/Soft-NMS/WBF
  -> 过滤低置信度和异常框
  -> 输出最终水平框结果
```

### 必须注意

- 测试图像尺寸不固定；
- 舰船可能是灰度图，飞机和发射车可能是彩色图；
- 测试时没有场景提示；
- 官方不会合并重复框；
- 不能让跨切片重复框进入最终结果；
- 边界目标和半出镜目标不能被简单删掉。

### 推荐实验维度

| 参数 | 可选范围 |
| --- | --- |
| 切片尺寸 | `1024`、`1280`、`1536`、`2048` |
| 重叠比例 | `0.15`、`0.20`、`0.25`、`0.30` |
| batch size | 根据显存调节 |
| NMS 阈值 | 分类别搜索 |
| score 阈值 | 全局和分类别搜索 |
| 后处理 | NMS、Soft-NMS、WBF |

### 产物

- 大图推理脚本；
- 坐标还原测试；
- 重复框统计；
- 大图可视化；
- 速度测试表。

### 退出标准

最大尺寸大图能够稳定完成推理，输出坐标正确，重复框受控。

## 阶段 P8：召回率提升

### 目标

使三大类平均 Recall 达到并超过 `85%`。

### 主要手段

- 提高输入分辨率；
- 优化切片尺寸；
- 增大切片重叠；
- 降低部分类别阈值；
- 强化小目标增强；
- 少数类过采样；
- Copy-Paste；
- 外部数据增广；
- 多尺度训练；
- TTA；
- 多模型融合。

### 诊断方法

每次实验必须回答：

- 哪些类别漏检最多；
- 哪些场景漏检最多；
- 是小目标漏检还是类别混淆；
- 是切片边界漏检还是模型没识别；
- 是阈值过高还是模型置信度本身低。

### 产物

- 漏检案例集；
- 少数类召回表；
- 阈值搜索表；
- 召回率提升实验记录。

### 退出标准

本地验证中三大类平均 Recall 稳定超过 `85%`，并保留一定安全余量。

## 阶段 P9：虚警率压低

### 目标

在 Recall 达标的前提下，将 FDR 控制到 `20%` 以下。

### 主要虚警来源

- 跨切片重复框；
- 背景建筑；
- 港口设施；
- 码头和集装箱；
- 普通车辆；
- 舰船尾迹或阴影；
- 机场建筑和地面纹理；
- 类别错判。

### 主要手段

- 全局 NMS；
- 分类别 score 阈值；
- 分类别 NMS 阈值；
- 困难负样本训练；
- 普通车辆负样本；
- 背景切片采样；
- 异常尺寸过滤；
- 长宽比过滤；
- 模型集成后去重。

### 实验原则

不要用简单提高阈值牺牲大量召回来压虚警。所有压虚警实验都必须同时记录 Recall 变化。

### 产物

- FP 案例库；
- 重复框统计；
- 阈值/NMS 搜索表；
- 困难负样本清单；
- Recall-FDR 平衡曲线。

### 退出标准

本地验证中三大类平均 FDR 稳定低于 `20%`，且 Recall 仍达标。

## 阶段 P10：速度优化与工程封装

### 目标

单幅最大尺寸图像推理时间不超过 `20 秒`，并具备统一测评入口。

### 优化顺序

1. 先保证结果正确；
2. 再进行批量切片推理；
3. 开启 FP16；
4. 优化切片数量；
5. 优化后处理；
6. 减少不必要的 TTA；
7. 如有必要导出 ONNX/TensorRT；
8. 最后做 Docker 封装。

### 必测场景

- 小图；
- 中图；
- 最大 `10000 x 10000` 图；
- 灰度舰船图；
- 彩色飞机图；
- 彩色发射车图；
- 目标密集图；
- 目标稀疏图。

### 产物

- 速度测试表；
- 统一推理入口；
- Dockerfile；
- 环境依赖说明；
- 模型加载说明；
- 输出格式说明。

### 退出标准

在接近官方硬件的环境下，大图推理时间达标，且无需人工选择模型或场景。

## 阶段 P11：研究报告沉淀

### 目标

让报告不是最后临时写，而是从日常实验记录自然生成。

### 报告必须覆盖

- 赛题理解；
- 数据集分析；
- 标注问题与修正；
- 25 类不均衡统计；
- 模型总体框架；
- 大图切片策略；
- 小目标检测策略；
- 类别不均衡策略；
- 外部数据使用说明；
- 后处理与去重；
- 推理速度优化；
- 消融实验；
- 可视化案例；
- 失败案例；
- 工程复现说明。

### 素材来源

| 报告内容 | 来源 |
| --- | --- |
| 数据统计 | P2 数据检查 |
| 标注问题 | 标注问题记录表 |
| 实验表格 | 实验记录表 |
| 消融实验 | E 系列实验 |
| 可视化 | TP/FP/FN 案例库 |
| 工程说明 | README、Docker 文档 |
| 外部数据 | 外部数据来源记录 |

### 退出标准

初赛提交前不需要重新追问“我们做过哪些实验”，所有结论都能从记录中找到证据。

## 阶段 P12：初赛提交

### 目标

按官方要求提交完整、可运行、可复现的作品。

### 提交材料

- 研究报告；
- 可运行算法代码；
- 模型权重；
- 推理脚本；
- 评估脚本；
- README；
- 环境依赖；
- 测试结果文件；
- 可视化示例；
- 报名表；
- 数据修正说明；
- 外部数据使用说明。

### 提交前检查

提交前必须完成 [提交材料清单.md](提交材料清单.md)。

### 退出标准

任意评审人员根据 README 能完成环境搭建、模型推理和结果生成。

## 阶段 P13：官方反馈迭代

### 目标

根据官方测评反馈快速定位问题。

### 注意

官方反馈可能只到大类指标，不一定给 25 类细节。

因此每次官方反馈后，要回到本地验证体系做对照：

- 是船、飞机还是发射车拖后腿；
- 是 Recall 不足还是 FDR 超标；
- 是速度不达标还是模型精度不足；
- 哪些本地场景与官方反馈最接近。

### 产物

- 官方反馈记录；
- 对应本地实验记录；
- 下一轮提交计划。

### 退出标准

每一次官方反馈都能转化为下一轮明确实验，而不是盲目换模型。

## 阶段 P14：决赛准备

### 目标

准备终审擂台赛、答辩和可能的新类别/新数据。

### 必做事项

- 关注官方是否新增类别；
- 若新增类别，快速纳入类别映射和训练流程；
- 整理最终技术路线；
- 提炼创新点；
- 准备答辩 PPT；
- 准备演示视频或可视化；
- 准备工程部署说明；
- 准备数据修正亮点；
- 准备失败案例与后续改进。

### 决赛重点

决赛评分更重视：

- 方案合理性；
- 技术创新程度；
- 工程可落地性；
- 实际部署潜力；
- 技术路线严谨性。

### 退出标准

团队能清楚回答：

1. 为什么这么设计；
2. 数据问题怎么处理；
3. 指标提升来自哪里；
4. 如何保证大图速度；
5. 如何部署到统一测评环境；
6. 相比普通检测方案创新在哪里。

## 5. 实验管理制度

## 5.1 实验编号

每次实验必须有唯一编号：

```text
EYYYYMMDD-序号-简短名称
```

示例：

```text
E20260825-001-baseline-yolo
E20260826-002-labels-v1
E20260827-003-copy-paste-planes
E20260828-004-global-nms
E20260829-005-fp16-speed
```

## 5.2 一次实验只验证一个主要假设

好实验：

```text
只比较 labels_v0_raw 和 labels_v1_fixed 对指标的影响。
```

坏实验：

```text
同时换模型、换标签、换输入尺寸、换增强、换阈值，然后不知道哪个因素有效。
```

如果必须做组合实验，也要先完成单因素实验。

## 5.3 实验必须记录的字段

每次实验至少记录：

- 实验编号；
- 实验目标；
- 负责人；
- Git commit；
- 数据版本；
- 标注版本；
- split 版本；
- 外部数据；
- 模型；
- 预训练权重；
- 输入尺寸；
- 切片尺寸；
- 重叠比例；
- 增强策略；
- 采样策略；
- 损失函数；
- 训练轮数；
- 推理阈值；
- NMS/WBF 参数；
- 是否 TTA；
- 是否 FP16/ONNX/TensorRT；
- 25 类指标；
- 三大类指标；
- 总体门槛指标；
- 推理时间；
- 误检分析；
- 漏检分析；
- 结论；
- 下一步。

模板见 [实验记录表.md](实验记录表.md)。

## 5.4 数据版本管理

建议版本命名：

```text
raw_v0
labels_v1_fixed_ship
labels_v2_fixed_plane
labels_v3_reviewed_all
split_v1_scene
split_v2_balanced
external_v1_ship
external_v2_plane
```

规则：

- 原始数据不改；
- 修正标签另存；
- 每个标签版本有说明；
- 每个 split 版本固定清单；
- 外部数据记录来源和许可证；
- 实验记录必须写清数据版本。

## 5.5 模型和配置管理

建议配置文件命名：

```text
configs/train/E20260825-001-baseline.yaml
configs/infer/E20260828-004-global-nms.yaml
configs/dataset/dataset_25cls_split_v1.yaml
```

规则：

- 不把关键参数只写在命令行里；
- 每次重要实验保留配置；
- 模型权重文件名包含实验编号；
- 最终提交权重单独复制到 `submissions/` 对应版本。

## 5.6 指标管理

实验指标至少分四层：

### 细分类指标

25 类分别统计：

- Recall；
- FDR；
- TP；
- FP；
- FN；
- 平均置信度；
- 重复框数；
- 漏检样例；
- 误检样例。

### 大类指标

统计：

- 舰船 Recall/FDR；
- 飞机 Recall/FDR；
- 发射车 Recall/FDR。

### 刚性门槛指标

统计：

- 三大类平均 Recall；
- 三大类平均 FDR；
- 最大图推理时间。

### 工程指标

统计：

- 单图平均推理时间；
- 最大图推理时间；
- 显存占用；
- 模型大小；
- Docker 镜像大小；
- 冷启动时间；
- 批量推理吞吐。

## 5.7 实验评审机制

每周至少做一次实验评审：

1. 本周完成了哪些实验；
2. 哪些实验有效；
3. 哪些实验无效；
4. 哪些指标最差；
5. 哪些数据需要复核；
6. 下周只保留最重要的 3 到 5 个实验；
7. 是否需要更新报告素材。

每次评审后更新：

- 实验记录表；
- 标注问题记录表；
- 提交材料清单；
- 项目 TODO。

## 6. 团队分工建议

| 角色 | 主要职责 |
| --- | --- |
| 项目负责人 | 排期、分工、风险控制、提交把关 |
| 数据负责人 | 数据检查、标注复核、类别映射、外部数据 |
| 算法负责人 | 模型训练、增强、不均衡策略、消融实验 |
| 工程负责人 | 推理接口、大图切片、Docker、速度优化 |
| 评估负责人 | 官方指标复现、实验表格、误检漏检分析 |
| 报告负责人 | 研究报告、答辩材料、可视化案例 |

小团队可以一人兼任多个角色，但每项任务必须有明确负责人。

## 7. 周期计划

### 第 1 周：项目骨架

- 建仓；
- 固定资料；
- 完成类别映射；
- 完成数据检查脚本；
- 完成可视化脚本；
- 建立实验记录制度。

### 第 2 周：基线闭环

- 自划分验证集；
- 跑通第一版模型；
- 复现本地 Recall/FDR；
- 输出第一批误检漏检图；
- 确定主要短板。

### 第 3 周：数据修正

- 复核舰船和发射车；
- 修正漏标错标；
- 建立标签版本；
- 对比 raw 与 fixed；
- 形成报告素材。

### 第 4 周：召回提升

- 小目标增强；
- 类别均衡；
- 少数类增强；
- 外部数据预训练；
- 阈值搜索。

### 第 5 周：虚警控制

- 困难负样本；
- 重复框去除；
- 分类别 NMS；
- 分类别阈值；
- 背景误检分析。

### 第 6 周：大图推理和速度

- 完成大图切片；
- 完成坐标还原；
- 完成跨切片融合；
- 完成 FP16/ONNX/TensorRT 尝试；
- 完成 20 秒压力测试。

### 第 7 周：提交准备

- 固定最终模型；
- 固定提交配置；
- 完成 Docker；
- 完成研究报告；
- 完成可视化示例；
- 完成提交材料清单。

### 第 8 周：官方反馈和冲刺

- 根据官方反馈定位问题；
- 小范围迭代；
- 不做高风险大改；
- 冻结最终版本；
- 提交。

实际排期以官方最终时间为准。

## 8. 风险控制

| 风险 | 可能后果 | 控制措施 |
| --- | --- | --- |
| 任务按三类做 | 子类错误导致 TP 不成立 | 全流程按 25 类训练和评估 |
| 标签不修 | 漏标错标污染训练 | 建立标注复核和版本管理 |
| 只看 mAP | 与官方 Recall/FDR 不一致 | 本地复现官方指标 |
| 重复框过多 | FDR 超标 | 全局 NMS/WBF 和重复框统计 |
| 大图推理太慢 | 不过刚性门槛 | 尽早做 1w 图压力测试 |
| 多模型太复杂 | 工程不稳定 | 单模型基线先稳定，再加融合 |
| 外部数据混乱 | 报告不可复现 | 记录来源、许可证和映射 |
| 官方反馈不细 | 难定位问题 | 本地输出 25 类细指标 |
| Docker 太晚 | 提交前环境崩 | 中期开始封装 |
| 报告最后补 | 证据不足 | 每周沉淀实验表和案例图 |

## 9. 最终交付物

### 代码交付

- 数据处理脚本；
- 训练脚本；
- 推理脚本；
- 评估脚本；
- 后处理脚本；
- Dockerfile；
- 环境依赖；
- 类别映射；
- 配置文件。

### 模型交付

- 最终权重；
- 推理配置；
- 阈值配置；
- NMS/WBF 配置；
- 模型说明。

### 文档交付

- 研究报告；
- README；
- 运行说明；
- 数据说明；
- 外部数据说明；
- 标注修正说明；
- 实验记录摘要；
- 可视化示例。

### 结果交付

- 官方要求格式的检测结果；
- 可视化结果；
- 速度测试记录；
- 本地评估记录。

## 10. 最终执行原则

本项目后续要坚持：

```text
先管理，再优化；
先闭环，再创新；
先复现指标，再追求高分；
先单因素实验，再组合策略；
先保证速度和可提交，再堆复杂模型。
```

最终目标不是做一堆零散实验，而是形成一条可解释、可复现、可部署、可答辩的完整比赛方案。

