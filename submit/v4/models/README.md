# Model payload

正式构建使用 `best.pt`，来源为：

`runs/detect/runs/train/exp004_original_continue40/weights/best.pt`

SHA-256：

`90B39F94D272648781152F28EF840B553D1AF64441815522319E7461C756048B`

权重文件体积较大且由 `.gitignore` 排除，不上传 GitHub。构建镜像前必须将哈希一致的文件放到本目录并命名为 `best.pt`。
