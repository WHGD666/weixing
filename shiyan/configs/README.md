# configs

所有正式实验都必须从配置文件启动，不把关键参数只放在命令行里。

目录：

- `dataset/`：数据路径、类别映射、标签版本、split 版本。
- `protocol/`：任务协议、评估协议、候选选择规则。
- `model/`：模型结构和预训练权重配置。
- `train/`：训练超参、增强、采样策略。
- `infer/`：推理、切片、阈值、NMS/WBF 配置。
- `eval/`：指标、IoU 阈值、聚合方式配置。
