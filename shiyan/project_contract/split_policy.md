# Split Policy v0

状态：draft，等待数据审计后冻结。

## 原则

- 不按切片随机划分。
- 优先按原始图像、场景或数据源划分。
- 验证集必须覆盖舰船、飞机、发射车。
- 少数类尽量在验证集中有代表性样本。
- holdout 一旦冻结，只有最终候选模型能评估一次。

## 需要保存

- split 文件清单。
- split 生成命令。
- split 随机种子。
- split 指纹。
- 每类样本在 train/val/holdout 中的分布。
