# One-command environment for the competition baseline.
#
# Scope:
# - Windows/Linux + NVIDIA GPU are the main target.
# - macOS can install CPU PyTorch through the Darwin-marked lines, but it is not
#   intended for serious training in this competition.
# - This file intentionally excludes MMDetection, Detectron2, TensorRT, and other
#   heavy research/deployment stacks. Add them only in a separate environment.
#
# Recommended Python: 3.10

--extra-index-url https://download.pytorch.org/whl/cu121

# Deep learning framework, pinned for reproducibility.
torch==2.5.1+cu121; platform_system == "Windows" or platform_system == "Linux"
torchvision==0.20.1+cu121; platform_system == "Windows" or platform_system == "Linux"
torch==2.5.1; platform_system == "Darwin"
torchvision==0.20.1; platform_system == "Darwin"

# Main detection baseline.
ultralytics>=8.3,<9.0

# Image IO, numerical computing, tables, and configs.
opencv-python>=4.8,<5.0
pillow>=10.0,<12.0
numpy>=1.26,<2.0
pandas>=2.1,<3.0
pyyaml>=6.0,<7.0
tqdm>=4.66,<5.0
tifffile>=2024.8

# Dataset audit, augmentation, evaluation, and COCO-format output.
albumentations>=1.4,<2.0
pycocotools>=2.0.7,<3.0
jsonschema>=4.21,<5.0
scikit-learn>=1.4,<2.0

# Large-image inference and box fusion.
sahi>=0.11,<0.12
ensemble-boxes>=1.0,<2.0

# Experiment notes, visualization, logs, and local verification.
matplotlib>=3.8,<4.0
seaborn>=0.13,<0.14
rich>=13.7,<15.0
loguru>=0.7,<1.0
pytest>=8.0,<9.0
ipykernel>=6.29,<7.0
jupyterlab>=4.1,<5.0
tensorboard>=2.16,<3.0
psutil>=5.9,<8.0
tabulate>=0.9,<1.0
