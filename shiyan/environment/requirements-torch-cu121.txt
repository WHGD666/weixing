# PyTorch CUDA dependency layer for Windows/Linux + NVIDIA GPU.
# This is kept separate for staged installs. For one-command setup, prefer
# shiyan/environment/requirements.txt.
#
# If the official contest platform later requires another CUDA runtime, create a
# new torch requirements file instead of silently editing old run records.

--index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
torchvision==0.20.1+cu121
