# Pretrained model

Place the official Ultralytics `yolo11x.pt` checkpoint in this directory before
running the EXP006 preflight. Record its SHA256 in the generated run manifest.
Do not substitute a differently named model without creating a new experiment.
