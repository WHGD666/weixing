# Frozen candidates

Only `scripts/07_freeze_candidate.py` should place model binaries here. Each
checkpoint must have a neighboring JSON manifest with its source path, SHA256,
metric evidence, configuration hashes, and freeze time.
